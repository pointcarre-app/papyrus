/**
 * @file Main entry point for the PCA Papyrus application.
 *
 * This file imports and initializes the main application logic.
 *
 * @module index
 */
import { initializeApp } from './core/app-init.js';

document.addEventListener('DOMContentLoaded', () => {
    initializeApp();
}); 